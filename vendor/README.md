# Optional local decoder

By default MS Studio loads `vgmstream-cli.js` and `vgmstream-cli.wasm` from `https://vgmstream.org/web/`. To self-host the decoder, place those two files here and change the Decoder base field in Audio Asset Vault to `./vendor/`.
